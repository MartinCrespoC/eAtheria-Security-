# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

.IA
.AGENTS.node_modules

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# mcp server
mcp-server/node_modules
mcp-server/dist

# E2E / Demo data (NEVER commit)
/scripts/e2e/
/uploads/
/demo-data/

# BugHunter knowledge vendor clone
vendor/

# Baileys (WhatsApp) per-session device credentials — NEVER commit
.baileys_auth/
# Legacy whatsapp-web.js session/cache (kept ignored during transition — the
# old folder still holds linked-device credentials) — NEVER commit
.wwebjs_auth/
.wwebjs_cache/
.tmp/

# Local fresh-install Postgres data dir
.pgdata/
# Pentest scratch files
gen-srp-pentest.ts
pentest-login.cjs
